# プロジェクトの概要

Business Contest KING 実行委員会の公式ホームページ（現行サイト: https://www.waav-king.org/ ）。
現行サイトの内容を React で作り直している。React + Vite + Tailwind CSS を使う。

## よく使うコマンド

- `npm install` — 初回の準備
- `npm run dev` — 開発サーバー（http://localhost:5173）
- `npm run build` — 本番ビルド（PR を出す前に通ること）
- `npm run lint` — oxlint

## 作るページ（現行サイトに合わせる）

- トップ（ミッション・ビジョン・バリュー、団体紹介）
- KINGについて
- コンテスト
- 過去のコンテスト
- 協賛企業の皆様
- お問い合わせ
- 特定商取引法に基づく対応
- プライバシーポリシー

## 二人で作業するときのルール

**`main` に直接コミット・push しない。** `main` は公開用の元データとして扱う。

1. 作業の前に `main` を最新にする（`git checkout main` → `git pull`）
2. 作業用ブランチを作る（例: `feature/about-page`、`fix/header-layout`）
3. 小さな単位でコミットする。メッセージは「何を変えたか」が分かるように書く
4. `npm run build` が通ることを確認してから push する
5. GitHub で Pull Request を作り、相方が確認してから merge する
6. 担当がかぶらないよう、触るページ・ファイルを事前に分ける

## 触ってはいけないもの

- `.env` や `.env.local` などの秘密情報は Git に入れない（`.gitignore` 済み）。共有は LINE などで直接渡す
- 個人情報（メンバーの連絡先など）や、団体外に出していない情報をコードや PR に書かない
- ロゴや写真は、団体として使ってよいものだけを入れる

## 公開について

- 公開の仕組み（GitHub Pages など）は未設定。現行の `waav-king.org` は、新サイトが完成して団体で確認してから切り替える
- ドメインの切り替えは管理者の作業が必要。勝手に進めない

## コードの書き方

- 既存のファイルの書き方（命名、コメントの量、Tailwind の使い方）に合わせる
- 変更は依頼された範囲にとどめる。ついでの整理は別の PR にする
