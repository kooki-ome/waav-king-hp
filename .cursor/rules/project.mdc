---
description: プロジェクト共通のルール
alwaysApply: true
---

このプロジェクトの構成・コマンド・二人での作業ルールは @CLAUDE.md に書いてある。必ず従うこと。

特に重要なこと:

- `main` に直接コミット・push しない。作業用ブランチを作り、Pull Request で出す
- `.env` などの秘密情報を Git に入れない
- push の前に `npm run build` が通ることを確認する
- 既存のコードの書き方に合わせ、依頼された範囲だけを変更する
